import styled from "styled-components";

export const GrayText = styled.span`
  color: ${props => props.theme.colors.gray};
`;

export const UppedRarefiedText = styled.div`
  text-transform: uppercase;
  letter-spacing: 0.5px;`;